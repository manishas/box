language: none

build:
  ci:
    - echo 'CI Running'
    - export IMAGE_NAME=ship-dtr.in/sdemo/box
    - ./build.sh

  post_ci:
    - ./push.sh

integrations:
  hub:
    - integrationName: "ship-ddc-dtr"
      type: "Docker Trusted Registry"
      branches:
        only:
          - master
